# 软件升级迭代记录

## 一、项目简介

**数据管理系统（Excel_Manager）** — 单文件 PySide6 (Qt 6.11) + SQLite 多用户数据管理系统。

- **主程序**：`Excel_Manager.py`（唯一最终更新版，也是 Nuitka/PyInstaller 编译源）
- **Python**：3.10.11
- **数据库**：`user_manager.db`（用户表）+ 每用户 `user_*.db`（data/config 表，WAL 模式）
- **依赖**：PySide6、pypinyin、chardet（条码/二维码/PIL/openpyxl 为延迟导入）
- **打包**：`0_nuitka_pyside6_clang_auto.bat`（Nuitka + PySide6 插件，输出 build_output）

## 二、维护约定（★ 每次改动必须遵守）

1. **主程序 / 打包脚本**：`Excel_Manager.py` 是唯一最终更新版，`0_nuitka_pyside6_clang_auto.bat`（Nuitka + PySide6 插件，输出 `build_output\Excel_Manager.dist\`）只编译该文件。
   - **★ v6.4 起：不必每次改动都编译**。日常小改只做「改源码 → 改文档 → 改版本号 + `version\` 备份」；**攒到需要分发 exe 时再编译一次即可**（编译约 2~3 分钟）。
   - **但只要改动影响打包，必须在同一次改动里同步改 bat**：新增数据文件 → `--include-data-files=源=目标`；新增依赖 → `--include-module` / `--include-package-data`；改入口/图标/输出目录 → 同步对应参数。
   - bat 现状（v6.4）：开头 `cd /d "%~dp0"`（任何位置调用都能编译）；**编译前用 PowerShell 检查 `Excel_Manager.exe` 是否在运行，占用时明确报错并退出**（避免链接阶段 `scons: *** ... Error 1`，实测会踩）；支持 `set EM_SKIP_BUILD=1` 做干跑自检（只跑检查、不编译）。
   - **bat 编码铁律：纯 ASCII 文本 + CRLF + 无 BOM**（cmd 按字节解析，UTF-8 中文注释配合中途 `chcp` 会让行边界错位）；写盘用 `[IO.File]::WriteAllText($p,$t,(New-Object Text.UTF8Encoding($false)))`。
2. **文档 + 版本号同步（强制）**：每次修改主程序后必须
   ① 同步更新本文件（「六、详细迭代记录」「四、完整功能清单」「五、架构分析」三处）；
   ② 更新 `ProjectInfo.VERSION`（约 276 行，窗口标题 / 关于对话框 / 帮助文档均引用它）；
   ③ 在 `ProjectInfo.VERSION_HISTORY`（约 285 行）追加本次版本说明。
3. **版本号更新必须备份**：每次改版本号，必须把主程序复制到 `version\` 子目录，命名 `Excel_Manager - X.X 简短说明.py`。
4. **脚手架入子目录**：脚手架/测试脚本必须放 `_scaffold\` 子目录（现有 `smoke_test_settings.py`），不散落根目录。
5. **设置实时自动保存 + 持久保存（★ v6.1 起强制）**：程序级全局设置存 `excel_manager_settings.json`（程序目录/打包后 exe 目录，`settings_path()` L366 / `load_global_settings` L378 / `save_global_settings` L387 原子写入 tmp+os.replace）；登录窗口用 500ms 防抖定时器 `_settings_save_timer` + `_autosave_settings()`（L2827）+ `_save_settings_now()`（L2832，原子写入 tmp+os.replace）；`closeEvent` 强制保存；启动 `load_global_settings()` 恢复。**新增设置项必须「保存/加载成对实现」**。
6. **行号基准**：本文件行号以 VS Code 编辑器为准（文件为纯 `\n` 换行，`Get-Content`/`split('\n')` 统计会偏小）。

## 三、当前版本信息

- **版本**：6.8（2026-09-24）
- **作者**：杜玛 | **License**：GNU AGPL v3.0 | **URL**：https://github.com/duma520
- **版本引用点**：窗口标题（MainWindow.init_ui 中 `ProjectInfo.NAME + VERSION + BUILD_DATE`）、`show_about`、`show_about_v2`（遍历 VERSION_HISTORY）、`ProjectInfo.get_header()`。

## 四、完整功能清单（v6.8）

| 功能 | 说明 |
|---|---|
| 多用户管理 | 增删用户、修改用户名（含 db 改名）、批量导入用户、用户信息显示（创建/最后登录时间） |
| 列配置修改（★ v6.4~v6.7） | **登录窗口与登录后主窗口两个入口**均可改列配置（主窗口：「工具→🧩修改列配置」或「🛠️用户设置→🧩修改列配置」）：不重建（清空）数据库，迁移表结构保留已有数据（新增/删除/改类型列、按中文名/位置自动识别改名列），改前自动备份；多列「唯一/必填」可正常保存。**★ v6.7：确认框直接列出「保留数据的列/新增列/会被删除的列」；完全对不上时直接中止（不再静默清空）；迁移后核对表结构** |
| 列配置与表结构一致性（★ v6.7 新增） | 列配置**一律以数据库真实表结构为准**（启动/录入前/打开列配置前自动校正，并把 `users.settings` 回写一致）——不会再出现「表格显示错位/满是 None」或添加数据报 `table data has no column named xxx` |
| 提示方式（★ v6.8） | **所有提示都在状态栏，不弹窗**：`notify()` 统一入口——成功/信息走临时消息，**警告/错误走状态栏右侧常驻提示**（不会被常规状态刷新盖掉、点击可清除、完整原文在 tooltip、绿/橙/红分色）；弹着对话框时闪一下标题；找不到状态栏才退回**非阻塞**弹窗 |
| 数据录入/编辑/删除 | 表格式录入、弹窗编辑、校验（必填/唯一/EAN13）、拼音首字搜索；**添加成功后只在状态栏提示，不弹窗（★ v6.4）**；**添加成功后列表自动翻到最新行（末行）并选中它（★ v6.6）**；**NULL 单元格显示为空串而不是「None」（★ v6.7）** |
| 条形码/二维码 | EAN13/Code128 条码、任意文本二维码、生成图片、批量导出图片、复制为图片 |
| 数据导入 | 超快速/批量导入（多线程）、字段自动映射、CSV/Excel/TXT/JSON、多编码检测（chardet/ANSI/GBK）、大文件生成器读取、无值守批量导入、统一配置应用到所有文件 |
| 麻将规则导入 | 专门 JSON 导入，自动建用户+列配置 |
| 多格式备份解析 | 检测并解析 SQLite/JSON/CSV/SQLServer 等格式备份文件 |
| 数据导出 | CSV/JSON 导出、打印详情、复制（文本/整行/带表头/图片） |
| 搜索 | 拼音增强搜索（数据量自适应三方案）、搜索所有列、关键字高亮 |
| 数据库备份/恢复 | 自动备份（2 小时定时 + 异步启动备份）、手动备份、保留 30 份、恢复对话框 |
| 重复数据处理 | 按字段查重、预览、保留策略（保留最新/最旧/全部）、清理前自动备份 |
| 数据管理 | 归档旧数据、VACUUM（含 >100MB 自动维护）、定期维护（24 小时） |
| 表头排序 | 点击表头排序（含排序状态指示） |
| 分页显示 | 每页 100 条 |
| 快捷键 | Ctrl+N 添加 / Ctrl+F 搜索 / Ctrl+E 编辑 / Delete 删除 / F5 刷新 |
| 右键菜单 | 编辑/删除/复制/导出/图片/详情/打印/刷新等 |
| 多线程架构 | QThreadPool（最大 4 并发）+ 进度对话框，加载/导入/搜索/导出/备份均异步 |
| 线程安全加固（★ v6.3 新增） | 工作线程专用独立数据库连接（不再与主线程共用 sqlite3 Cursor）；QThread 取消+保活+延迟释放；退出前等待所有线程结束 |
| 界面刷新稳定（★ v6.3 新增） | 统计/详情面板清空改用 `takeAt`+`deleteLater`（`_clear_layout`），不再破坏堆内存 |
| 全局设置持久化（★ v6.1 新增） | 窗口几何/最大化、上次登录用户、统一导入配置 → `excel_manager_settings.json` 实时自动保存 |
| 帮助→使用说明（★ v6.2 新增） | `ProjectInfo.HELP_TEXT` 接入帮助菜单「📖使用说明」项（`show_help`），修复死代码 |

## 五、架构分析（代码地图）（行号以 v6.8 为准，总行数 11047）

> 行号用 `Select-String -Path Excel_Manager.py -Pattern '^class X'` 重新取过（旧版本文档的部分行号已偏移）。

### 5.0 统一提示机制（★ v6.8，行号 57~215）
| 函数/类 | 行号 | 用途 |
|---|---|---|
| find_status_bar(obj) | 57 | 沿“对象 status_bar 属性 → QMainWindow.statusBar() → parent 链 → 活动窗口”找可用状态栏 |
| NotifyLabel | 91 | 状态栏右侧**常驻**提示标签（点击清除），专放警告/错误 |
| _ensure_notify_label | 111 | 每个状态栏只建一个常驻标签（addPermanentWidget） |
| _flash_dialog_title | 121 | 有模态对话框时闪一下标题（状态栏被遮挡时也能看见） |
| _fallback_popup | 142 | 找不到状态栏时的兜底弹窗（对象形式 + show，**非阻塞**） |
| notify | 176 | **统一提示入口**：info/success → 临时消息；warn/error → 常驻标签（与 LOGO 同一处定义 NOTIFY_ICONS/MSEC/COLORS） |

### 5.1 多线程/Worker 层（128~355）
| 类 | 起始行 | 用途 |
|---|---|---|
| WorkerSignals | 46 | 工作线程信号定义 |
| BaseWorker | 54 | QThreadPool 轻量一次性任务 |
| ThreadedWorker | 87 | QThread 重型任务基类（支持进度/中断/**release_db 105**） || LoadDataWorker | 118 | 数据加载线程（用完关闭私有连接） |
| ImportDataWorker | 143 | 数据导入线程（分批，**自己开 private 连接**） |
| SearchDataWorker | 205 | 数据搜索线程 |
| ExportDataWorker | 225 | CSV 导出线程 |
| BackupWorker | 245 | 数据库备份线程 |
| BarcodeGenerator | 265 | 条码/二维码生成器 |

### 5.2 信息/常量/全局设置
| 项 | 起始行 | 用途 |
|---|---|---|
| ProjectInfo | 274 | 项目元数据（VERSION 276 / BUILD_DATE 277 / VERSION_HISTORY 285 / HELP_TEXT 312） |
| MacaronColors | 373 | 马卡龙色系常量 |
| settings_path() | 399 | 全局设置文件路径（程序目录/exe 目录） |
| load_global_settings() | 411 | 加载全局设置 |
| save_global_settings(data) | 420 | 原子写入全局设置（tmp+os.replace） |

### 5.3 数据库层
| 类 | 起始行 | 关键方法 |
|---|---|---|
| UserManager | 626 | add_user / delete_user / get_user_settings / get_user_db_file / update_user_settings（立即 commit）/ update_last_login / update_username / close |
| UserDatabase | 722 | __init__（**private 参数：工作线程/迁移专用独立连接**）/ initialize_database（**新建/重建库，会清空数据**）/ _save_columns_config_to_db / **migrate_column_config 904（保留数据的表结构迁移）** / get_columns_config 1028 / **get_table_columns 1098 / get_column_labels / column_migration_preview（★ v6.7）** / insert_data / _add_pinyin_to_data / _enable_pinyin_system / _open_private_connection / **clone_for_thread** / backup_database（WAL，保留30份）/ restore_from_backup / quote_identifier（防注入）/ batch_insert_data / bulk_insert_data / archive_old_data |
| DatabaseConnectionManager | 1652 | SQLite 连接池（WAL、30s 超时、跨线程）——**主线程专用，不给工作线程共用**；`UserDatabase(db_file)` 拿到的是池化共享连接，**close() 会关掉它**（详见 8. 维护要点） |
| **plan_column_migration** | 1885 | ★ v6.7 模块级函数：算出「新列配置 ↔ 旧表列」的对应关系（列名 → 中文名 → **位置兜底（仅当前两轮全空时）**），**迁移与确认框预演共用** |
| **duplicate_column_names** | 1956 | ★ v6.7：查重名列 |
| **column_config_change_preview** | 1969 | ★ v6.7：生成「保留数据的列/新增列/会被删除的列」预览文案（供确认框） |
| **apply_column_config_migration** | 1995 | ★ v6.5 模块级函数：改列配置的公共流程＝备份 → 迁移表结构（**private 连接**）→ **核对表结构 == 新配置（★ v6.7）** → 保存用户配置；登录窗口与主窗口共用 |

### 5.4 导入对话框
| 类 | 起始行 | 关键方法 |
|---|---|---|
| FastImportDialog | 2051 | init_ui 2058 / setup_field_mapping / auto_map_fields / preview_data / check_data_quality / get_import_config / validate_and_accept（**必填字段确认框固定为 question**） |
| UltraFastImportDialog | 2884 | __init__（自动加载统一配置）/ **init_ui 2946** / **save_unified_config**（保存后持久化到全局设置文件）/ load_unified_config / auto_load_unified_config / apply_unified_config_automatically / 无人值守 auto_accept_for_unattended |

### 5.5 窗口/对话框
| 类 | 起始行 | 关键方法 |
|---|---|---|
| LoginWindow | 3233 | __init__（加载全局设置 + 500ms 防抖定时器）/ init_ui 3261 / _restore_window_state / _autosave_settings / _save_settings_now / **closeEvent 3490**（强制保存）/ login / **modify_column_config 3371**（选用户改列配置：预填→对话框→**带影响预览的确认框**→apply_column_config_migration，★ v6.7 调整了顺序）/ add_user / delete_user（**先取真实库文件名再删，★ v6.7**）/ import_users / import_rules_json / 多格式备份解析 parse_* / read_import_file / ultra_fast_import_data |
| ImportUsersDialog | 5877 | 批量导入用户勾选对话框 |
| ColumnConfigDialog | 5969 | 列配置对话框（**v6.5 起支持 parent**）/ **add_column 6018（★ v6.7 默认名避重）/ remove_column 6074（★ v6.7 不许删到最后一列）** / get_columns_config 6085 / **load_existing_config 6114（★ v6.7 空配置也保留一行）** |
| MainWindow | 6182 | __init__（**★ v6.7 列配置以真实表结构为准 + 回写 settings**）/ **_heal_settings_columns_config 6266 / _sync_columns_config_with_db 6283（★ v6.7）** / init_ui 6582（状态栏 6653） / **_background_backup 6303 / _start_pool_worker 6327** / **线程生命周期：_cleanup_worker 6367 / _retire_worker 6398 / _purge_retired_workers 6423** / **_clear_layout 6435** / **closeEvent 6461** / create_menu_bar 6807 / **load_data 6921 / _on_data_loaded 6940 / _scroll_to_newest_row 6967（★ v6.6）/ _table_column_index 7014 / _value_by_name 7029 / _update_table_with_data 7037（★ v6.7 按列名取值 + NULL→空串）** / **update_stats 7141** / search_all_columns 7275 / display_search_results 7304 / **add_data 7470（★ v6.7 录入前同步列配置；添加成功只写状态栏 + 记 rowid）** / **show_item_detail 7584** / export_data 7769 / **show_user_settings 7800** / **modify_columns_config 7846（★ v6.5/v6.7）** / show_help 7937 / **manual_backup 8807** / cleanup_duplicate_data 10665 / 归档/VACUUM / 右键菜单 / 打印复制 |
| EditDataDialog | 11047 | 编辑数据表单对话框 |

### 5.6 入口（11143）
`if __name__ == '__main__':` → QApplication → UserManager → LoginWindow → **按 `_start_maximized` 决定 show/showMaximized** → app.exec()

## 六、详细迭代记录

### 6.8（2026-09-24）★ 所有提示改到状态栏（不再弹窗）

**用户原话**：「把所有弹窗提示改成在状态栏提示。」

改动（全文件 **146 处** 提示类弹窗一次改完）：
1. **新增统一入口 `notify(owner, message, level, msec)`（176）+ 配套函数**：
   - `find_status_bar(obj)`（57）：从对象自身 `status_bar` 属性 → `QMainWindow.statusBar()` → 沿 `parent()` 向上 → `QApplication.activeWindow()`，逐级找可用状态栏。
   - `NotifyLabel`（91）/`_ensure_notify_label`（111）：状态栏右侧的**常驻提示标签**。为什么不用 `showMessage` 存重要提示？因为程序里很多地方会拿 `showMessage` 刷常规状态（“正在加载…”“用户: X | 总记录数: N”），临时区会被盖掉 —— 实测重要错误刚提示就被下一次刷新冲没了。常驻标签不会被盖，点一下就能清掉。
   - `notify()` 规则：`info`/`success` → 临时消息（带 ℹ️/✅，5s）；`warn`/`error` → 常驻标签（⚠️/❌，橙/红色，warn/error 同时 print 到控制台便于排查）；多行折叠成一行、超 300 字截断，**完整原文放 tooltip**。
   - `_flash_dialog_title`（121）：有模态对话框时（导入/列配置等）状态栏被挡住，就把对话框标题加上提示图标闪一下（时长同消息），再恢复。
   - `_fallback_popup`（142）：实在找不到状态栏（极少数独立窗口/启动早期）才退回弹窗，且用 **QMessageBox 对象形式 + `show()`（非阻塞）**，绝不用 `exec()`。
2. **批量改造**：新增脚手架 `_scaffold\convert_msgbox_to_notify.py`（先备份原文件再改、保留 LF/无 BOM、改完 py_compile 自检）。它只改**正好 3 个参数**的 `information/warning/critical`，并且跳过“返回值被赋值”的调用（如 `reply = QMessageBox.warning(...)` 后面要按按钮分支）；**question/about 不动**。结果：146 处改完，1 处人工处理（`validate_and_accept` 里的必填字段确认框，本来就是 Yes/No 选择，已改为 `QMessageBox.question`）。
3. **★ 踩到的坑（已写进脚本注释）**：改造脚本把我 helper 里兜底用的 `QMessageBox.critical(owner, '错误', text)` 也换成了 `notify(...)` → **无限递归**。所以兜底弹窗改成对象形式（`QMessageBox(...)` + `box.show()`），不用那三个静态方法。
4. **保留的弹窗（11 处 question + 1 处 about）**：删除用户/删除数据/退出当前用户/覆盖安装/迁移确认等**需要用户选“是/否”**的，以及“关于”。这些不能写状态栏（否则操作会默默发生或默默取消）。
5. 顺手把 `smoke_test_exit_bug.py` 里两处“固定睡 60×10ms”的等待改成**等到条件满足或超时**，消除机器忙时的偶发失败（曾出现 `currentRow=-1 rows=70` / `活跃线程 left=14` 的假失败）。

- 新增回归测试 `_scaffold\smoke_test_notify.py`（**33 项全部通过**）：源码级断言（全文件已无 information/warning/critical 静态调用、notify 调用点 ≥140、question 保留、兜底是对象形式+show）+ info 写临时消息 + warn/error 写常驻标签且**不被常规状态刷新盖掉** + 颜色/图标/tooltip + 沿 parent 链找状态栏 + 对话框闪标题 + 找不到状态栏时兜底不阻塞。
- 旧套件回归：`smoke_test_column_sync.py` 43 项、`smoke_test_exit_bug.py` 33 项、`smoke_test_migrate_columns.py` 25 项、`smoke_test_settings.py` 全 0 失败。

### 6.0（2026-08-20）
版本号升级至 6.0，持续优化性能与稳定性。

### 6.1（2026-08-20）★ 全局设置实时自动保存 + 持久化
- **新增全局设置文件 `excel_manager_settings.json`**（`settings_path()` L366 / `load_global_settings` L378 / `save_global_settings` L387，原子写入 tmp+os.replace）。
- **登录窗口 LoginWindow**：启动加载全局设置并恢复窗口几何/最大化（`_restore_window_state` L2802）、上次登录用户自动选中（`load_users` L2837）；新增 500ms 防抖定时器 `_autosave_settings()`（L2811）→ `_save_settings_now()`（L2816）；`closeEvent`（L2830）强制保存；登录成功记录 `last_user`（`login` L2872）。
- **主窗口 MainWindow**：启动恢复 `main_geometry`/`main_maximized`/`global_unified_config`（`__init__` L5493）；`closeEvent`（L5587）保存窗口几何/最大化 + 统一导入配置。
- **超快速导入**：`save_unified_config`（L2363）保存统一配置时同步持久化到全局设置文件，重启后仍可自动沿用。
- **持久化键**：`login_geometry` / `login_maximized` / `main_geometry` / `main_maximized` / `last_user` / `global_unified_config`。
- 冒烟测试：`_scaffold\smoke_test_settings.py`（离屏验证 防抖保存→写盘→新窗口恢复 全链路通过）。

### 6.2（2026-08-20）★ 修复三处已知问题
1. **install_package / subprocess**：顶部新增 `import subprocess`；`install_package` 定义前置到 import 区；移除原先无效的 pypinyin/chardet 自动安装 try 块（原逻辑在定义前调用 install_package 且引用未导入的 subprocess，从未正常工作过；pypinyin/chardet 已改为顶层无条件导入，依赖已在 Nuitka 编译脚本内置）。`install_package` 保留为工具函数（按需调用）。
2. **重复数据清理对话框 selected_fields 作用域错误**（`cleanup_duplicate_data` L9562）：`selected_fields` 提升到外层作用域（`nonlocal`），`preview_btn`/`start_btn` 改用 `on_preview`/`on_cleanup` 函数并增加空字段保护（未选字段时弹窗提示）。
3. **HELP_TEXT 死代码**：新增 `show_help`（L6839），把 `ProjectInfo.HELP_TEXT`（.format 替换版本/作者/URL 占位符）接入帮助菜单「📖使用说明」项（L5985），死代码变为可用功能。
- 冒烟测试补充：install_package 定义/subprocess、HELP_TEXT 占位符替换、selected_fields 修复 三项验证全部通过。

### 6.3（2026-09-23）★ 修复「添加资料，点确定后程序自动退出」

**现象**：添加数据点「确定」后，程序有时毫无提示地直接消失（无错误弹窗、无崩溃提示），数据往往已经写入。

**排查证据链**：
1. Windows 应用日志：`python.exe` 多次在 `pyside6.abi3.dll` 访问违例（0xC0000005）；用户实际运行的 `Excel_Manager\Excel_Manager\Excel_Manager.exe`（2026-08-08 编译的 v5.1.1）异常终止时无 WER 记录，但用户库 `-wal` 未回收、`closeEvent` 备份没生成 → 属于**非正常退出**。
2. 用真实用户库副本离屏压测（`_scaffold\repro_add_data.py`）**稳定复现**：先出现 `QThread: Destroyed while thread '' is still running`（退出码 0xC0000409），后续复现为新建 `LoadDataWorker` 时访问违例（0xC0000005）。
3. 逐项二分（只加载 / 只添加不加载 / 不刷表格 / 不刷统计 / 严格串行）定位到**两处独立缺陷**：
   - **A（主因：堆内存被踩坏）**：`update_stats()` / `show_item_detail()` 用 `widget().setParent(None)` 清空布局 —— 控件只被变成顶级窗口，**布局项仍指向它**；随后 Python 侧释放控件，布局里留下悬空指针，下一次布局/重绘就踩坏堆内存，之后随便一个操作（典型是下一次「添加资料→确定」时新建 LoadDataWorker 线程）进程直接退出。
   - **B（线程生命周期）**：`ThreadedWorker.finished` 是自定义信号，在 `run()` 内部就 emit，此时 QThread 线程往往还没结束；`_cleanup_worker()` 直接丢掉 Python 引用会析构 C++ QThread → Qt `qFatal` 终止进程；回调里用 `self.load_worker`（最新引用）而不是信号发送者，还会清错对象、让运行中的线程失去引用。

**修复内容**：
1. **新增 `MainWindow._clear_layout(layout)`（5728）**：`takeAt(0)` 逐项摘下布局项 → 控件 `hide()+deleteLater()`、子布局递归清理 → `del item` 释放布局项。`update_stats`（6357）与 `show_item_detail`（6787）改用它（全文件已无 `setParent(None)`）。
2. **线程生命周期加固**：`_cleanup_worker`（5660）先 cancel + `wait()`，再把引用放进 `_retired_workers` 保活，延迟 1s 由 `_purge_retired_workers`（5716）释放；新增 `_retire_worker`（5691）——替换 `self.xxx_worker` 引用前必须先保活旧线程；新增 `_cleanup_finished_workers`（5707）批量清理已结束线程；`load_data`/`search_all_columns`/`export_data`/`manual_backup`/`execute_ultra_fast_import` 替换引用前均先 `_retire_worker`。
3. **回调改用真正的发送者**：`_on_data_loaded`（6228）/`_on_load_error` 用 `self.sender()` 判定，旧任务的迟到结果直接丢弃。
4. **退出前等线程结束**：`closeEvent`（5754）在 `cancel()` 后对所有活跃/待回收线程 `wait(3000)`，避免退出瞬间析构运行中的 QThread 而崩溃。
5. **工作线程专用数据库连接**：`UserDatabase.__init__(db_file, private=False)`（520）+ `_open_private_connection`（1029）+ `clone_for_thread`（1041）；`ThreadedWorker.release_db`（97）在线程结束时关闭私有连接；`LoadDataWorker`/`SearchDataWorker`/`ExportDataWorker`/`BackupWorker` 一律传入 `self.user_db.clone_for_thread()`，`ImportDataWorker` 内部改用 `UserDatabase(db_file, private=True)`；**后台自动备份**也改为 `_background_backup`（5622）用独立连接（原来会报「Cannot operate on a closed database」）。**禁止再让工作线程共用主窗口那条池化连接的 Cursor**（sqlite3 的 Cursor 无线程保护）。

- **回归测试**：`_scaffold\smoke_test_exit_bug.py`（offscreen + 真实库副本，v6.3 时 19 项，v6.4 扩到 **21 项全部通过**）；压测脚本 `_scaffold\repro_add_data.py`（修复前 23~72 次必崩，修复后 250 次全过）。

### 6.5（2026-09-24）★ 登录后也能修改列配置
1. **主窗口新增两个入口**（与登录窗口的入口等价）：
   - 「🛠️工具 → 🧩修改列配置」（`create_menu_bar`）
   - 「🛠️用户设置」窗口里新增「🧩修改列配置」按钮（`show_user_settings` 7269，点按钮后关掉设置窗再打开列配置）
   - 新方法 `MainWindow.modify_columns_config`（7315）：预填当前配置 → 确认（说明保留规则 + 改前自动备份）→ **先 cancel/wait 掉正在跑的加载/搜索线程**（避免它们握着旧表结构）→ 迁移 → 刷新（`self.columns_config`、表头、统计、详情、状态栏）。
2. **抽出公共流程** `apply_column_config_migration(db_file, username, user_manager, new_columns_config)`（1661）：备份 → `migrate_column_config` → 保存用户配置；登录窗口与主窗口共用，行为一致。
3. **★ 修掉一个会导致「改完列配置后主窗口报 Cannot operate on a closed database」的真 bug**：`apply_column_config_migration` 原来用 `UserDatabase(db_file)`，而它会从连接池拿到**与主窗口同一个连接对象**，随后 `close()` 把主窗口正在用的连接也关了；改为 `UserDatabase(db_file, private=True)`（独立连接）。
4. `migrate_column_config` 的拼音开关改为**从库里的 `config.pinyin_enabled` 读取**（private 连接不走 `_init_pinyin_columns`，否则重建表后拼音列不会补回来，拼音搜索会失效）。
5. `ColumnConfigDialog.__init__` 支持传入 `parent`（登录后从主窗口打开时居中/模态跟随）。
6. **测试脚本安全加固**：所有脚手架脚本在构造任何对象前先 `os.chdir(临时目录)` 并断言（`UserManager()` 用的是 CWD 相对的 `user_manager.db`，从真实程序目录运行会把真库当测试库）。
- 回归测试：`_scaffold\smoke_test_exit_bug.py` 扩到 **28 项**（新增「登录后改列配置」7 项：列配置已更新/数据条数不变/中文名生效/表头重建/已落库/表结构含新列）；`smoke_test_migrate_columns.py` 25 项；`smoke_test_settings.py` 全过。

### 6.4（2026-09-23）★ 添加数据不弹窗 + 修改列配置不再清空数据
1. **添加数据成功后不再弹窗**（`add_data` 6901）：成功时只 `status_bar.showMessage('✅ 数据添加成功', 5000)`，失败时状态栏提示；校验（必填/唯一）与异常仍保留弹窗（否则用户不知道为何没存进去）。顺带去掉了嵌套事件循环（原来弹窗期间又会触发表格刷新）。
2. **「登录窗口→修改列配置」不再清空数据**（`LoginWindow.modify_column_config` 2970）：原来是 `initialize_database()`（内部 `DROP TABLE data`）重建，**所有数据会丢**；现改为新增的 **`UserDatabase.migrate_column_config`（710）**：
   - 列名相同 → 直接沿用；
   - 列名变了但**中文名(label)没变** → 视为重命名，`ALTER TABLE ... RENAME COLUMN` 改名并保留数据；
   - 新列 → 建列（旧记录该列为空）；删列 → 只丢该列数据；改类型 → 重建表并搬数据；
   - 搬迁时连 `rowid` 一起搬，记录标识不变；
   - 迁移前 `backup_database(backup_type="manual")` 自动备份；
   - 迁移后重建 config 配置、补拼音列、重建索引；
   - 所有列都换了列名+中文名（无法对应）时，保留记录条数（该情况只能力所能及）。
3. **修复多列「唯一/必填」写配置时的主键冲突**：新增 `_save_columns_config_to_db`（669）一律用 `INSERT OR REPLACE`，并把唯一/必填改为 **JSON 列表**（`unique_columns` / `required_columns`）存储，支持多列；`get_columns_config`（833）读新键并兼容旧的单值键；顺手清理已删列的标签/条码/二维码残留键。
4. `_enable_pinyin_system`（929）跳过 `*_pinyin` 列，不再生成 `xxx_pinyin_pinyin` 垃圾列。
- 回归测试：`_scaffold\smoke_test_migrate_columns.py`（**25 项全部通过**：合成库 改名/新增/删除/改类型/多列唯一必填 + 真实用户库副本 1245 条数据逐值比对不变）；`_scaffold\smoke_test_exit_bug.py` 扩到 **21 项**（新增「添加成功不弹窗 + 状态栏有提示」断言）。

### 6.6（2026-09-24）★ 添加数据后自动翻到最新行
1. **需求**：添加一条资料后，列表自动定位到刚添加的那条（最新记录），不用再手动往下拖。
2. **实现**（三处，均在 `MainWindow`）：
   - `__init__` 新增标志 `self._scroll_to_last_after_load = False` 与 `self._pending_scroll_rowid = None`（5805）；
   - `add_data` 改为接收 `insert_data()` 返回的 **rowid**（`new_rowid = self.user_db.insert_data(data)`，7003），成功时置 `_scroll_to_last_after_load = True` + `_pending_scroll_rowid = new_rowid`（7009/7010）再 `load_data()`；
   - 新增 `_scroll_to_newest_row()`（6522），在 `_on_data_loaded`（6495）末尾（状态栏提示之后）调用：读标志→**立即复位**→优先按 `_pending_scroll_rowid` 在首列 `Qt.UserRole` 里逐行找（最多 rowCount 次，即使启用了表头排序也不会找错）→ 找到就滚到该行、找不到退化为末行 → `scrollToItem(item, QAbstractItemView.PositionAtBottom)`（无 item 时 `scrollToBottom()`）→ `selectRow(row)` + `setCurrentCell(row, 0)`，全程 try/except。
3. **为什么放 `_on_data_loaded` 而不是 `add_data`**：表格是在加载回调里重建的（`_update_table_with_data` 重写 `setRowCount`），在 `add_data` 里立即滚会被随后的重建冲掉；且每次加载都是全量重建（重新按 rowid 升序填），所以旧行对象不能留，只能记 rowid 到回调里再定位。
4. **只对「添加数据」生效**：标志位默认 False，刷新（F5）/搜索/导入/编辑/删除等刷新表格的路径不会自动跳行（回归测试有专门断言）。
5. **向后兼容**：主窗口不是由 `MainWindow.__init__` 建的（未定义这两个属性）时也不会报错 —— 读取处用 `getattr(self, ..., None/False)`。
6. **顺手保留数据**：`insert_data()` 本来就返回 `lastrowid`（1011），原来调用处丢弃了，现在拿来用，无需额外查询。
- 回归测试：`_scaffold\smoke_test_exit_bug.py` 扩到 **33 项**（新增 5 项：添加后选中末行 / 选中行 rowid 最大 / 待回位 rowid 已消费 / 标志已复位 / 普通刷新不置位）。

### 6.7（2026-09-24）★ 修复「增加列后全是 None / 添加不了数据」（含一次真实数据丢失）

**用户原话**：「增加列以后，不能直接用吗？全是 none，还以为数据都没了，结果重开才能正常显示，但是，添加不了数据。」

**只读探针查出的事实**（真库 + backups，勿重复排查）：
- `user_定位器ID.db`：data 表只有 **1 列 `定位器ID`**，70 条真实数据（WAL 已 checkpoint，就是当前状态）。
- `user_manager.db → users.settings`（用户 `定位器ID`）：却是**默认两列** `column_1/列 1`、`column_2/列 2` —— **配置与表结构分裂**。
- 备份时间线（决定性证据）：`_auto_074420`=`[定位器ID]` 有数据 → `_manual_074500`=迁移前备份 → **`_auto_074510`=`['column_1','column_2']` 全 NULL（70 条数据被清空）** → `_auto_074546`=又恢复成 `[定位器ID]`（用户自己恢复/重导）→ `_auto_074737` 保持。

根因（三个真 bug）：
1. **迁移会静默清空数据**：`migrate_column_config` 在“新列一列都对不上旧列”时走兜底分支，只 `INSERT ... DEFAULT VALUES` 保持记录条数 —— **60 行→70 行全变 NULL，而且提示还是“已有 70 条数据已保留”**。用户就是在列配置对话框里得到默认列名（`column_1/列 1`）后确认，触发了这条路径。
2. **`users.settings` 与真实表结构可以分裂**：主窗口直接用 `json.loads(settings)` 当列配置（只在自己为空时才回退表结构），于是恢复旧备份后配置指向 `column_1` 而表里只有 `定位器ID` → 表格按位置错位/显示 None，且**添加数据的 INSERT 必然报 `table data has no column named column_1`**。
3. **NULL 被显示成文本 “None”**：`QTableWidgetItem(str(row_values[i]))` 把 NULL 渲染成 `None`，火上浇油（用户以为整表数据没了）。

修复（v6.7）：
1. **新增共用匹配规划 `plan_column_migration`（1694）**：列名相同 → 中文名相同（视为改名）→ **位置兜底**（**仅当前两轮一列都没配上时才启用** —— 用户把列名+中文名一起换掉时仍能保住数据；已有列能配上就不再做位置兜底，否则会把“被删列”的旧值串到“新增列”里。《迁移》与《确认框预览》共用同一函数，不会两边不一致。
2. **迁移不再静默清空（713）**：挪到任何 DDL 之前先算好对应关系，`rowcount > 0 且一列都对不上` → **直接返回 False 中止**；兜底分支保留但会打日志 + 在返回说明里写“各列数据已清空”。
3. **`apply_column_config_migration`（1804）迁移后核对** `get_table_columns() == 新配置列名`，不等就报失败——避免“配置改了但表没改”的分裂状态。
4. **列配置一律以真实表结构为准**：`get_columns_config()`（837）不再在 config 表为空时 `return None`；`MainWindow.__init__` 用 `get_columns_config()` 的结果，发现与 settings 不一致就**回写 settings 自愈**（`_heal_settings_columns_config` 6081）；另加 `_sync_columns_config_with_db`（6098）用在**添加数据前**、**打开列配置对话框前**（不一致时状态栏提示并自动修正）。历史遗留的不一致状态下一次启动就自动修好，无需手工改库。
5. **表格按列名取值 + NULL→空串**：新增 `_table_column_index`（6829）/ `_value_by_name`（6844），`_update_table_with_data`（6852）与 `display_search_results` 改用它们。
6. **对话框与确认框防护**：`ColumnConfigDialog.remove_column`（5889）不允许删到最后一列；`add_column`（5833）默认列名/中文名避重；`load_existing_config`（5929）拿到空配置也保留一行；`get_columns_config` 后查重名列（`duplicate_column_names` 1765）；确认框用 `column_config_change_preview`（1778）列出**保留数据的列 / 新增列 / 会被删除的列**；登录窗口把确认框从“对话框之前”挪到“对话框之后”（先看实际变更再确认）。
7. 顺手修：`LoginWindow.add_user` 建库改用 `UserDatabase(db_file, private=True)`（原来拿池化连接再 close() 会关掉共享连接）；`delete_user` 先取真实库文件名再删（原来用 `user_名.db` 拼，改名用户会删错文件）。

- 新增回归测试 `_scaffold\smoke_test_column_sync.py`（**43 项全部通过**）：`plan_column_migration` 8 例（含“用户实际场景”与“新增列不得拿走被删列数据”）+ 迁移数据安全（旧版这一格全是 None）+ **分裂状态端到端**（自愈/回写 settings/表格值正确/NULL 显示空串/真实走 add_data 能插库且无错误弹窗/v6.6 自动跳行不回退）+ 对话框防护。
- 旧套件回归：`smoke_test_exit_bug.py` 33 项 0 失败；`smoke_test_migrate_columns.py` **25 项 0 失败**（其中“新增列 stock 旧记录为空”这条正好反向验证了“位置兜底不能过激”）；`smoke_test_settings.py` 全过。
- 排查用只读探针（留着以后用）：`_scaffold\probe_user_db_config.py`（对比每个 user_*.db 的表结构与 config 表、users.settings）、`probe_users_table.py`、`probe_locator_backups.py`（列备份的表结构/首行）、`probe_autoscroll.py`。

## 七、设置持久化规范（★ v6.1 起）

- **程序级全局设置** → `excel_manager_settings.json`（`settings_path()` 程序目录/exe 目录）。键：`login_geometry`/`login_maximized`/`main_geometry`/`main_maximized`/`last_user`/`global_unified_config`。
- **实时自动保存机制**：LoginWindow 持有 `_settings_save_timer`（500ms 单次防抖）→ `_autosave_settings()` 触发 → `_save_settings_now()` 原子写盘；`closeEvent` 先 stop 再强制保存。
- **用户级设置** → `user_manager.db → users.settings` 字段（JSON 列配置）：`UserManager.update_user_settings`（L448）每次 UPDATE 立即 commit（实时写盘）；列配置修改（`modify_column_config`/`add_user`/`import_rules_json`）走此通道。
- **库级设置** → 每用户 `user_*.db → config` 表（key/value）：列属性（必填/唯一/条码/二维码/拼音开关）由 `UserDatabase.initialize_database`（L647）即时 commit。
- **新增设置项必须「保存/加载成对实现」**：改 `ProjectInfo` 无关；全局设置项需在 `_save_settings_now()` 写入 + 对应 `load_global_settings()`/`__init__` 处恢复。

## 八、维护要点 / 已知问题

- **★ 改列配置铁律（v6.4~v6.7）**：三个入口（登录窗口按钮 / 主窗口「工具→🧩修改列配置」/「用户设置」里的按钮）**都必须走 `apply_column_config_migration()`**；它内部用的是 **`UserDatabase(db_file, private=True)` 独立连接** —— **绝不能用 `UserDatabase(db_file)` 再 `close()`**，因为那会从连接池拿到与主窗口同一个连接对象，一关主窗口就报 `Cannot operate on a closed database`。`initialize_database()` 会 `DROP TABLE data`，**只能用于新库**（add_user / 导入用户 / 导入麻将规则）。
- **★ 列配置一致性铁律（v6.7）**：**列配置只有一个真源＝data 表的真实结构**（`UserDatabase.get_columns_config()` / `get_table_columns()`），`users.settings` 只是缓存，不一致时必须按表结构纠正并回写。**迁移的列匹配一律用 `plan_column_migration()`**，不要在别处写第二套匹配逻辑；**绝不允许“把现有数据清成 NULL 但保留条数”的兜底**（这个坑已经真的毁过一次用户数据）。表格填值一律走 `_value_by_name()`（按列名 + NULL→空串），不要用 `str(row_values[i])`。
- **★ 测试脚本铁律（v6.5）**：`UserManager()` 用的是 **CWD 相对的 `user_manager.db`**，所以脚手架脚本必须**先 `os.chdir(临时目录)` 再构造任何对象**（并断言 cwd 在 temp 下）。2026-09-23 曾因终端命令里的相对路径 `.\user_manager.db`（工具省略了 `Set-Location`）误删真实 `Excel_Manager\user_manager.db`（已从测试副本恢复 12 个用户）—— **清理/复制一律用绝对路径**。
- **★ 提示方式铁律（v6.8 起）**：新增提示一律用 `notify(self, '文案', 'info'|'success'|'warn'|'error')`，**禁止**再写 `QMessageBox.information/warning/critical`（有回归测试扫源码）。`QMessageBox.question` 仅用于“必须让用户决定”的确认；模式对话框里提示时 notify 会自动闪标题。控制台里的 print 保留（warn/error 会打一份，便于排查）。**「添加后自动跳行」只能由 `add_data` 置标志、由 `_on_data_loaded` 消费并复位**（不要直接在 `add_data` 里滚动，表格会在加载回调里重建）；新增其它「操作后定位」需求照此套路加标志位，避免刷新/搜索也乱跳。
- **★ 线程安全铁律（v6.3）**：① 工作线程必须用 `self.user_db.clone_for_thread()` 的独立连接，禁止把主窗口的 `user_db` 交给 QThread（共用 Cursor 会踩坏堆内存）；② 替换 `self.xxx_worker` 引用前必须先 `self._retire_worker(旧对象)`；③ 清空布局一律用 `MainWindow._clear_layout()`，**禁止 `setParent(None)`**；④ 退出前必须等线程结束（`closeEvent` 已处理）。
- **★ 测试脚本清单（v6.7）**：`smoke_test_column_sync.py`（**43 项**，列配置↔表结构一致性、迁移数据安全、分裂自愈、能录入）/ `smoke_test_exit_bug.py`（**33 项**，崩溃修复 + 不弹窗 + 自动跳行 + 登录后改列配置）/ `smoke_test_migrate_columns.py`（**25 项**，含真实用户库副本逐值比对）/ `smoke_test_settings.py`。跑法：`& "D:/Program Files/Python310/python.exe" -u _scaffold/<脚本>`（PowerShell 用 & 调用；控制台编码可能乱码，看各脚本自己的 `_smoke_*_out.txt`）。
- **★ 只读排查探针（v6.7）**：`probe_user_db_config.py`（列出每个 user_*.db 的 data 列/config 表/前两行 + users.settings 里的列配置，**定位“配置与表结构分裂”一拿就准**）、`probe_users_table.py`、`probe_locator_backups.py`（列 backups 里某用户的表结构与首行，**靠时间线能看出数据是何时被清/被恢复的**）、`probe_autoscroll.py`。全部用 `sqlite3.connect(f"file:{path}?mode=ro", uri=True)` 只读打开，不动真库。
- **★ 用户实际运行的可能是旧 exe**：`Excel_Manager\Excel_Manager\Excel_Manager.exe` 是 2026-08-08 编译的 **v5.1.1**；源码修复需重新编译（`0_nuitka_pyside6_clang_auto.bat` → `build_output\Excel_Manager.dist\`）并替换 exe 才能生效。**编译前先退出正在运行的 Excel_Manager.exe**（bat 已内置检查，占用时直接报错退出）；**v6.4 起不再每次改动都编译**，攒到需要分发时编译一次。
- **脚手架**：`_scaffold\smoke_test_settings.py`（全局设置持久化）、`_scaffold\smoke_test_exit_bug.py`（退出/线程/布局/弹窗/登录后改列配置 **28 项**）、`_scaffold\smoke_test_migrate_columns.py`（列配置迁移 **25 项**）、`_scaffold\repro_add_data.py`（添加资料压测，支持 `--mode ...`）、`_scaffold\probe_*.py`（Qt 行为探针）、`_scaffold\_tmp_check_*.py`（只读核对用户库）。
- **已知 Bug（均已修复，v6.2）**：①`install_package` 定义前调用 + 引用未导入的 `subprocess`（已修复：新增 `import subprocess`、函数前置、pypinyin/chardet 改顶层无条件导入）；②重复清理对话框 lambda 引用局部变量 `selected_fields`（已修复：nonlocal 提升 + 空字段保护）；③`ProjectInfo.HELP_TEXT` 死代码（已修复：接入帮助→使用说明 `show_help`）。
- **打包脚本**：主编译 `0_nuitka_pyside6_clang_auto.bat`（PySide6 插件，含 `--include-module=chardet/pypinyin/openpyxl/barcode/qrcode/PIL`）；`0_nuitka.bat` 用的是 **pyqt5 插件，疑似过时**（程序是 PySide6），编译请优先用前者。
- **启动流程**：登录窗口 → 登录成功 → MainWindow（延迟 200ms 加载数据，避免启动卡顿）。
- **目录**：`version\`=版本备份；`backups\`=数据库备份（最多30份）；`_scaffold\`=测试脚本；`build_output\`=Nuitka 输出；`Excel_Manager\`=旧打包输出（dist 勿动）；根目录 `user_*.db`/`user_manager.db`=运行时数据库。
